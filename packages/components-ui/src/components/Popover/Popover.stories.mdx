import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import { PlOutlinePlusCircle } from '@drogo-ui/icons';
import { Popover } from '.';

<Meta
  title="Popover/Popover"
  component={Popover}
  args={{
    trigger: 'Popover',
    children: 'Popover',
    className: '',
  }}
  argTypes={{
    colorContent: { control: 'color' },
  }}
  parameters={{}}
/>

export const Template = ({ ...args }) => <Popover {...args} />;

# Popover

<!-- Adicionar uma descrição para o componente -->

O componente `Popover` mostra um outro componente ou texto quando o usuário clica no elemento que o contém.

## Import

```sh
  import { Popover } from '@drogo-ui/components';
```

## Basic

<Canvas>
  <Story name="Default" args={{}} argTypes={{}} parameters={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Example

Utilização com ícone:

<Canvas>
  <Story
    name="Example"
    args={{
      trigger: (
        <span
          style={{
            width: 40,
            height: 40,
            borderRadius: '50%',
            background: '#fff',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontSize: 24,
            boxShadow: '2px 2px 2px 1px rgba(0, 0, 0, 0.2)',
          }}
        >
          <PlOutlinePlusCircle />
        </span>
      ),
    }}
    argTypes={{}}
    parameters={{}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Popover} />
