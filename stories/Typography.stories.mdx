import { Meta, Typeset } from '@storybook/addon-docs/blocks';

import { defaultTheme } from '@drogo-ui/styles';

<Meta title="Docs/Typography" />

# Typography

#### Font Family: Open Sans

<Typeset
  fontSizes={[
    '1rem',
    '1.2rem',
    '1.4rem',
    '1.6rem',
    '2rem',
    '2.4rem',
    '3.4rem',
    '4.8rem',
    '6rem',
    '9.6rem',
  ]}
  fontFamily={defaultTheme.font.family.primary}
  sampleText="With big powers come big responsabilities"
/>

#### Font Weights

<div
  style={{
    boxShadow: `${defaultTheme.shadow.base}`,
    border: '1px solid rgba(0,0,0,.1)',
    padding: '1rem 2rem',
    margin: '2.5rem 0 4rem',
  }}
>
  <p style={{ fontWeight: `${defaultTheme.font.weight.light}` }}>
    <span>300</span> - With big powers come big responsabilities
  </p>
  <p style={{ fontWeight: `${defaultTheme.font.weight.regular}` }}>
    <span>400</span> - With big powers come big responsabilities
  </p>
  <p style={{ fontWeight: `${defaultTheme.font.weight.medium}` }}>
    <span>500</span> - With big powers come big responsabilities
  </p>
  <p style={{ fontWeight: `${defaultTheme.font.weight.semiBold}` }}>
    <span>600</span> - With big powers come big responsabilities
  </p>
  <p style={{ fontWeight: `${defaultTheme.font.weight.bold}` }}>
    <span>700</span> - With big powers come big responsabilities
  </p>
  <p style={{ fontWeight: `${defaultTheme.font.weight.extraBold}` }}>
    <span>800</span> - With big powers come big responsabilities
  </p>
</div>
